# Examples

Example outputs now live inside each skill folder as `example-output.md`.

See `.claude/skills/company-context/example-output.md` for a complete worked example (Linear).
All other skills have placeholder files — run the skill, then save a good output as your reference.
